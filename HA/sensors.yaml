  - platform: time_date
    display_options:
      - 'time'
# -----------------------------------------------------------
# ------------- GMOTION -------------------------------------
# -----------------------------------------------------------
####### WILL BE OVERRIDDEN BY APPDAEMON ANYWAY ##############
  - platform: template
    sensors:
      g_motion_t:
        value_template: "loading"
      g_motion_cellar_t:
        value_template: "loading"
      g_motion_basement_t:
        value_template: "loading"
      g_motion_upstairs_t:
        value_template: "loading"
# ---------------------------------------------------------
      illuminum_kolja_t:
        value_template: "loading"
      illuminum_caro_t:
        value_template: "loading"
# -----------------------------------------------------------
# --------------- MQTT --------------------------------------
# -----------------------------------------------------------
# -----------------------------------------------------------
# -----------------------------------------------------------
  - platform: systemmonitor
    resources:
      - type: disk_use_percent
        arg: /home
      - type: memory_free
      - type: processor_use
# -----------------------------------------------------------
